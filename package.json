{
  "name": "usps-label-portal",
  "version": "1.0.0",
  "description": "A streamlined USPS label management portal for admins, resellers, and users",
  "main": "server/index.js",
  "scripts": {
    "start": "concurrently --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"npm run server\" \"npm run client\"",
    "server": "npm run free-server-port && nodemon server/index.js",
    "free-server-port": "lsof -ti:5001 | xargs kill -9 2>/dev/null || true",
    "free-client-port": "lsof -ti:3000 | xargs kill -9 2>/dev/null || true",
    "client": "npm run free-client-port && cd client && npm start",
    "serve": "node server/index.js",
    "build": "cd client && npm install --include=dev && npm run build",
    "install-client": "cd client && npm install",
    "install-server": "npm install",
    "install-all": "npm run install-server && npm run install-client"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "usps",
    "shipping",
    "labels",
    "mern",
    "portal"
  ],
  "author": "Label Flow",
  "license": "MIT",
  "dependencies": {
    "archiver": "^7.0.1",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.3.0",
    "multer": "^2.1.1",
    "nodemailer": "^8.0.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.1.10"
  }
}
